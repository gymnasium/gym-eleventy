---
layout: layouts/base.njk
pagination:
    data: data.items.bios
    size: 1
    alias: bio
permalink: "instructors/{{ bio.title | slugify }}/"
title: "{{ bio.title }} | Instructors | {{ site.title}}"
---
<h1>{{ bio.title }}</h1>
<h4>{{ bio.id }}</h4>
<p>{{ bio.description }}</p>
{{ content | safe }}
