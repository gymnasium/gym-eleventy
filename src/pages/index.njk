---
layout: layouts/main.njk
permalink: /
eleventyComputed:
  meta:
    title: "{{ config.meta.subtitle }}"
meta:
  sitemapPriority: 1.0
  sitemapChangeFreq: weekly
  bodyClass: home
---
{%- set hero = heros[featured['home']['hero']] -%}
{%- set heroClass = "home-hero bg-mono-800 full-bleed" -%}
{%- include 'modules/hero.njk' -%}

<section class="how-gymnasium-works">
  <h1>How Gymnasium Works</h1>
  <ul>
    <li>
      <h2>Free Online Courses</h2>
      <p>Our courses are taught by industry experts. You can enroll anytime,
        anywhere.</p>
    </li>
    <li>
      <h2>New Skills</h2>
      <p>You can share your achievement with your network when you pass a final
        exam.</p>
    </li>
    <li>
      <h2>Career Opportunities</h2>
      <p>We get you jobs through Aquent, one of the largest staffing agencies in
        the world.</p>
    </li>
    <li>
      <h2>Lifelong Learning</h2>
      <p>We produce articles, tutorials, and webinars with trailblazers in the
        web community.</p>
    </li>
  </ul>
</section>
<section class="home-featured-courses">
  <h1>Featured Courses</h1>
  <ul>
    {%- for item in featured['home']['courses'] -%}
      {%- set id = item | upper -%}
      <li>
        <div id="featured-{{ item }}">
          <img
            src="{{ config.urls.data }}{{ collections.catalog[id]['img'] }}"
            alt="Image for {{ collections.catalog[id]['title'] }}"
          />
          <span>{{ collections.catalog[id]["topic"] }}</span>
          <h3>{{ collections.catalog[id]["title"] }}</h3>
          <p>{{ collections.catalog[id]["description"] }}</p>
          <p>with {{ bios[collections.catalog[id]["instructor"]].name }}</p>
          <p>{{ bios[collections.catalog[id]["instructor"]].subtitle }}</p>
          <a class="btn" href="/courses/{{ collections.catalog[id]['title'] | slugify }}"
            >Learn More</a
          >
        </div>
        {# DEBUG:<br />
        {{ courses[id] | dump }} #}
      </li>
    {%- endfor -%}
  </ul>
  <p>Design a career you love with free online courses on design, development, UX,
    accessibility, and career skills.</p>
  
  <a class="btn" href="/courses/">View all Courses</a>
</section>

<section class="home-find-work bg-mono-700 full-bleed">
  <header>
    <h1>Find Work</h1>
  </header>
  {%- include 'modules/jobs.njk' -%}
</section>

<section class="as-seen-in">
  <header>
    <h1>As Seen In</h1>
  </header>
  <ul>
    {%- for i in range(1,6) -%}
    <li>
      <img src="https://placehold.co/200x50?text=logo {{ i }}" alt="Logo">
    </li>
    {%- endfor -%}
  </ul>
</section>